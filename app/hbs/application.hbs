<div class="main-side">
  <div class="top">
    {{#link-to 'like' class="bolinter-logo"}}Laforce{{/link-to}}
    <div class="slogan">an extension</div>
    <ul class="main-menu">
      <li>{{#link-to 'profile' user._id}}<img class="user-photo" {{bind-attr src="user.photo"}}> {{user.name}}{{/link-to}}</li>
      <li>{{#link-to 'like'}}Booster{{/link-to}}</li>
      <li>{{#link-to 'matches'}}Matches{{/link-to}}</li>
      <li {{action "reset"}} class="reset">Sign out</li>
    </ul>
  </div>
  <div class="side">
    {{outlet side}}
  </div>
</div>
<div class="page">
  {{outlet}}
</div>